(() => {
    // propiedad strictNullCheck en el tsconfig.json para que esto se pueda hacer (NO HACERLO, MANTENER SIEMPRE TS LO MAS ESTRICTO POSIBLE)
    // let isActive: boolean = undefined;

    // let isActive: boolean = null;
})();