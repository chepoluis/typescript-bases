(() => {
    /**
     * Se pueden crear incluso mas definiciones de tipos no solo tuplas (2)
     */
    const hero: [string, number] = ['Luis', 100];
    const hero2: [string, number, boolean] = ['Luis', 100, true];
})();